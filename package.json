{
  "name": "skinanalysis-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI Skin Analysis Website — facial skin condition classification with Grad-CAM explainability and skincare recommendations",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix frontend && npm run build",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server"
  }
}
